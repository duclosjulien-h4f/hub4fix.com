# Fichiers temporaires Word/Excel
~$*
~WRL*.tmp

# OS
desktop.ini
Thumbs.db
.DS_Store

# IDE
.vscode/
.idea/

# Node
node_modules/
dist/
build/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Docker
.docker/

# Env
.env
.env.local
.env.*.local
# Secrets de developpement wrangler (SESSION_SECRET, SLICER_SECRET...) : JAMAIS publies
.dev.vars
**/.dev.vars
# Etat local de wrangler (base D1 locale, caches)
.wrangler/

# Outillage generation d'images (script + sorties brutes — local uniquement)
genimg/
# References pipeline hotlist (dessins sources — depot PUBLIC, ne pas committer)
pipeline-hotlist/

# Claude Code (settings locaux - ne pas partager)
.claude/settings.local.json

# ============================================================
# CONFIDENTIEL — ce depot est PUBLIC (deploiement du site).
# Ne JAMAIS committer ici : documents business, data, IP, instructions.
# Ces fichiers restent sur le disque, simplement hors du suivi git.
# ============================================================
# Documents bureautiques
*.docx
*.xlsx
*.xlsm
*.pptx
*.pdf
# Dossiers internes
_coffre-fort/
*.hc
*.vc
documents/
context/
cloud-slicer/
leads-buffer/
input/
output/
images_pieces_detachees/
files/
2files/
# Data / sauvegardes / sources
*.zip
*.3mf
*.dxf
*.bak*
veille_casse*
hub4fix_veille_casse*
synthese*
synthese_hebdo*
speech*
# Images de recherche / generees (racine uniquement — pas les assets du site)
/Gemini_*
/dyson.png
/magimix.png
/roulette.png
/kenwood*
# Instructions internes Claude Code (gardees en local, pas en public)
CLAUDE.md
CLAUDE_CODE_BRIEFING.md
DEMO_SPEC.md
# Outillage / config Claude Code local (jamais public)
.claude/
# Strategie / veille / shadowlist (tous formats, partout)
*[Ss]ynthese*
*[Ss]hadow*
*[Rr]oadmap*
*pipeline_ia*
*veille*
# Exceptions a *veille* ci-dessus : la regle vise les DOCUMENTS de veille
# (strategie, shadow list), pas le code ni le suivi operationnel.
!pipeline-td/veille-reactivation.md
!veille.py
# Visuels generes par le pipeline hotlist (38 Mo). Le site NE les sert PAS depuis
# ici : les images publiques viennent de R2 via /img/h4f/<id>. Les committer
# alourdirait l'historique definitivement, sans rien apporter en ligne.
hub4fix.com/images/hotlist/

# Fichiers de travail a la racine (jamais publier sur le depot public)
TodoList.md
# Script de sauvegarde local : contient les chemins du poste (T:\, profil utilisateur)
sauvegarde.ps1
/scratch_*
/Note_*.md
/hotlist_scoring_*.csv
/.agents/
skills-lock.json
cartographie_usage_claude_code.md
/*.dxf.svg
/*.dxf.png
/hub4fix_logo_variante1_tricolore.svg

# Sortie de build-site.sh (site assemble) : jamais commitee
_site/
